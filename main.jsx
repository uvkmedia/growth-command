import React from 'react'
import ReactDOM from 'react-dom/client'
import GrowthCommand from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GrowthCommand />
  </React.StrictMode>
)
